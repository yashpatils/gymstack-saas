{
  "name": "gymstack-saas",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "typecheck": "npm --prefix frontend run typecheck",
    "predeploy": "npm --prefix frontend run lint && npm run typecheck && npm run build:frontend",
    "start:frontend": "npm --prefix frontend run start",
    "db:seed": "npm --prefix backend run db:seed",
    "preflight": "npm run preflight:frontend && npm run preflight:backend",
    "preflight:frontend": "cd frontend && npm ci && npm run lint && npm run typecheck && npm run build",
    "preflight:backend": "cd backend && npm ci && npm run prisma:validate && npm run build"
  }
}
